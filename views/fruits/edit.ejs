<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>
<body>
  <!-- update the header -->
  <h1>Edit <%=fruit.name%></h1>
  <form action="/fruits/<%=fruit.id%>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" id="name" placeholder="E.g Banana" value="<%= fruit.name %>"/>

    <label for="isReadyToEat">Ready to Eat?</label>
    <input type="checkbox" name="isReadyToEat" id="isReadyToEat" <% if(fruit.isReadyToEat){%> checked<% } %>/>
    <!-- update the button text -->
    <button type="submit">Update Fruit</button>
  </form>
  <a href="/fruits">Back to Fruits</a>
</body>
</html>